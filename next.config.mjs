/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  allowedDevOrigins: [
    "dc94706d2245f0.lhr.life",
    "surprise-web-live.loca.lt",
    "major-lands-care.loca.lt"
  ]
};

export default nextConfig;
