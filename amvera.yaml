meta:
  environment: nodejs
  toolchain:
    name: npm
    version: "20"

run:
  command: sh -c "node ./node_modules/next/dist/bin/next build && node ./node_modules/next/dist/bin/next start --hostname 0.0.0.0 --port ${PORT:-3000}"
  persistenceMount: /data
  containerPort: 3000
